export interface LoginOptions {
    username: string;
    password: string;
    code?: string;
    hwid?: string;
}
export interface LicenseOptions {
    license: string;
    code?: string;
    hwid?: string;
}